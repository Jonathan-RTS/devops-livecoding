name: Deploy Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-22.04

    steps:
      # 1. Récupère le code de votre dépôt
      - name: Checkout code
        uses: actions/checkout@v2

      # 2. Connexion à Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.jonathanr93 }} # Utilisation correcte du secret
          password: ${{ secrets.dckr_pat_4hQ7Cenq5UZcJJJdyYzBvebWppE }} # Utilisation correcte du secret

      # 3. Configure Docker Buildx pour permettre le build multi-plateforme
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 4. Build et pousse l'image Docker du backend
      - name: Build and push backend image
        uses: docker/build-push-action@v4
        with:
          context: ./simple-api          # Chemin vers votre projet backend
          push: true                     # Permet de pousser l'image vers Docker Hub
          tags: jonathanr93/backend:latest # Remplacez "jonathanr93" par votre nom d'utilisateur Docker Hub
